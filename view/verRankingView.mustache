<!DOCTYPE html>
<html lang="es">

<body class="bg-light">

<div class="container py-4">
    <h1 class="text-center mb-4">🏆 Ranking Global</h1>

    <div class="table-responsive">
        <table class="table table-hover align-middle">
            <thead class="table-dark">
            <tr>
                <th>#</th>
                <th>Foto</th>
                <th>Nickname</th>
                <th>Puntaje</th>
                <th>Pais</th>
            </tr>
            </thead>
            <tbody>
            {{#ranking}}
                <tr {{#esUsuarioActual}}class="mi-posicion"{{/esUsuarioActual}}>
                    <td>{{posicion}}</td>
                    <td>
                        <a href="perfil.php?id={{id}}">
                        <img src="/public/images/profiles/{{foto_perfil}}"   class="img-thumbnail rounded-3 shadow-sm"
                             style="height: 60px; width: auto; object-fit: cover;"
                             alt="Foto de {{nickname}}" >
                        </a>
                    </td>
                    <td>
                        <a href="perfil.php?id={{id}}" class="text-decoration-none">{{nickname}}</a>
                    </td>
                    <td>{{puntaje}}</td>
                    <td>

                    </td>
                     {{pais}}
                </tr>
            {{/ranking}}
            </tbody>
        </table>
    </div>

    <div class="mt-4 text-center">
        <p class="fw-bold">Tu posición actual: <span class="text-primary">{{miPosicion}}</span></p>
        <a href="lobby.php" class="btn btn-secondary">Volver al Lobby</a>
    </div>
</div>

<!-- Bootstrap JS Bundle -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
